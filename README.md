# test-github-codespace

```bash
./setup.sh
sudo passwd vscode
```

copy rclone.conf to /workspaces/rclone_config/.rclone.conf

```bash
./mount_and_clone.sh
```

port forward 2222
vscode Remote-SSH
Reopen devcontainer