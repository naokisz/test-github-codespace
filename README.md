# test-github-codespace

```bash
./setup.sh
```

port forward 2222
vscode Remote-SSH
open /workspaces
copy rclone.conf to /workspaces/rclone_config/.rclone.conf

```bash
./mount_and_clone.sh
```

Reopen devcontainer